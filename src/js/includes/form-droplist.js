const openListBtns = document.querySelectorAll(".droplist-toggler");
const lists = document.querySelectorAll(".tickets-form__droplist");
const listItem = document.querySelectorAll(".tickets-form__droplist-item");

for (const openListBtn of openListBtns) {
  openListBtn.addEventListener("click", () => {});
}
